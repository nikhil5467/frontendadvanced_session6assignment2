import {Component,Input, Output, EventEmitter} from '@angular/core';

const template: string = require('./newcomp.html');

@Component({
    selector: 'newcomp',
    template,
    directives: []
    })

export class newcomp {}